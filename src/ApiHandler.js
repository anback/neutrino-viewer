//@flow
const baseUrl = 'http://localhost:8000'

export const getQuotesForIdentifier = (identifier: string) => {
    // fetch(`${baseUrl}/quotes?identifier=${identifier}`)
    return Promise.resolve([])
}